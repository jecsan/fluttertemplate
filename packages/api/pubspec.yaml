name: remoteservice
description: remoteservice

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.1.0+1

environment:
  sdk: ">=2.12.0 <3.3.0"


dependencies:
  flutter:
    sdk: flutter

  # REST
  dio: ^4.0.0
  json_annotation: ^4.0.0
  colorize: ^3.0.0

  shared_preferences: ^2.0.5 # Simple Key-Value persistence(SharedPreference/NSUserDefaults)
  flutter_secure_storage: ^5.0.2 


  mockito: ^5.0.3
  freezed: ^2.1.0+1
  # mocktail: ^0.2.0

dev_dependencies:
  # Database Codegen
  build_runner: ^2.0.2
  json_serializable: ^6.1.4

  flutter_test:
    sdk: flutter
